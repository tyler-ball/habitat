expeditor:
  defaults:
    buildkite:
      timeout_in_minutes: 30

steps:
#######################################################################
# Release!
#######################################################################

  - label: "hab"
    command:
      - cd components/hab && hab pkg build .
    agents:
      queue: 'default-privileged'
    # timeout_in_minutes: 10
    # retry:
    #   automatic:
    #     limit: 1

  - label: "plan-build"
    command:
      - cd components/plan-build && hab pkg build .
    agents:
      queue: 'default-privileged'
    # timeout_in_minutes: 10
    # retry:
    #   automatic:
    #     limit: 1